# Shared package commands

# Start the shared chatbot example
start:
    @echo "🚀 Starting shared chatbot example..."
    uv run python src/pydantic_ai_shared/examples/chatbot.py

# Run tests
test:
    @echo "🧪 Testing shared package..."
    uv run pytest
    @echo "✅ Shared tests passed"
