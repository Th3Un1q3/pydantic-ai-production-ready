# Shared Package

Common utilities and code shared across Pydantic AI projects.

## Overview

This package contains:
- Shared utilities and helpers
- Common Pydantic models
- Reusable agent components
- Example implementations

## Usage

This package is automatically available to other packages in the workspace:

```python
from pydantic_ai_shared.examples import ChatbotExample
```

## Structure

```
shared/
├── src/
│   ├── __init__.py
│   └── examples/        # Example implementations
├── tests/
├── pyproject.toml
└── README.md
```
